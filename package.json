{
  "name": "pokemonll",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "npm run build && wrangler dev",
    "generate:pokemon-names": "node scripts/generate-pokemon-name-map.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "lucide-react": "^0.468.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "typescript": "^5.8.3",
    "vite": "^6.0.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.41.0",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "wrangler": "^4.101.0"
  }
}
